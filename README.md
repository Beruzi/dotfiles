# dotfiles
Some configurations aren't meant to live in `~/.config`... so, when cloning to a new machine, you'd need to set up a symlink in `~/` pointing to `~/.config/` for the following applications:
  - tmux
  - zsh

#### TODO
- [ ] Change the ugly ahh color chatGPT gave our zsh ssh status
