# dotfiles
Some configurations aren't meant to live in the ~/.config file... so when cloning to a new machine, you'd need to set up a symlink in ~/ ---> ~/.config/...
  - tmux
  - zsh

#### TODO
- [ ] Change the ugly ahh color chatGPT gave our zsh ssh status
